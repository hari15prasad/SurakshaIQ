export {
  NAV_ITEMS,
  MODULE_ROLES,
  getVisibleNavItems,
  getModuleForPath,
  hasRole,
  hasPermission,
  canAccessModule,
  canViewDistrict,
  canManageUsers,
} from 'utils/permissions';

export type { NavItem, ModuleKey } from 'utils/permissions';
